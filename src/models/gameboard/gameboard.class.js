export class Gameboard {
  constructor(grid, fleet) {
    this.grid = grid;
    this.missedAttacks = 0;
    this.fleet = fleet;
    // this.player = player;
  }

  receiveAttack(coordinates) {
    
  }

  areAllShipsSunk() {
    // TODO
  }

}